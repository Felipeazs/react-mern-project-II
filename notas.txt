git add .
git commit -m "
Frontend:
auth-slice: const register and login function, imported to Register to manage the states of the user data when register and login to the page.

"
git push origin -u main